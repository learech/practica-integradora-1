<div>
    <h1>LISTADO DE PRODUCTOS:</h1>
    <ol>
        {{#each products}}
        <li>
            <h2>Nombre: {{this.title}}</h2>
            <p>Descripcion: {{this.description}}</p>
            <p>Stock: {{this.stock}}</p>
            <p>Precio: {{this.price}}</p>
            <p>Codigo: {{this.code}}</p>
            <p>Id: {{this._id}}</p>
            <p>Img: {{#each this.thumbnails}}<img src="{{this}}">{{/each}}</p>
        </li>
        {{/each}}
    </ol>
</div>